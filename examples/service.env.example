# 复制到 /srv/hblglzh/secrets/<project>.env 后修改。
# 不要把真实 secret 提交到 Git。
NODE_ENV=production
HOST=127.0.0.1
PORT=11001
# DATABASE_URL=file:/srv/hblglzh/data/<project>/database.sqlite
# JWT_SECRET=CHANGE_ME
